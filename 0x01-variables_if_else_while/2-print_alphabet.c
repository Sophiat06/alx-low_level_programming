#include <stdio.h>
/**
 * main - print letters in lower case
 *
 * Return: Always 0 (success)
 */
int main(void)
{
	char l;

	for (l = 'a'; l <= 'z'; l++)

	{
		putchar(l);
	}

	putchar('\n');

	return (0);
}
